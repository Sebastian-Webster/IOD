import React from "react";

const Profile = () => {
    return (
        <>
            <h1>You are logged in at the moment!</h1>
            <button>Logout</button>
        </>
    )
}

export default Profile;