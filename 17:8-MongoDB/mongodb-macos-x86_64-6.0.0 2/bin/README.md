The MongoDB Community server is too big to push to GitHub so if you want to run the stuff in this folder download MongoDB Community server

Run it with
path/to/mongod/executable --dbpath path/to/dbData/folder